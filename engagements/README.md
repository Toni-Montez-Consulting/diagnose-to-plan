# Engagements

Private client engagement folders live here locally and are gitignored by default.

Use:

```powershell
.\.venv\Scripts\python.exe -m dtp kit new mom-nonprofit --project site-rebuild --kind launch
```

Do not commit client-private notes, credentials, raw screenshots, customer records, or internal financial details.
