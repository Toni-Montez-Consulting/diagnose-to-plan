# Working In This Repo

This repo is Toni's local consulting harness.

- Keep the CLI local-first and file-first.
- Do not add web UI, vector search, background daemons, or multi-agent runtime orchestration.
- Keep Phase 1 limited to `dtp draft` and `dtp skills --validate`.
- Do not write real skill bodies for `voice`, `pricing`, or `sow`; Toni owns those.
- Never write to sibling repos from this harness.
